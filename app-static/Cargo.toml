[package]
name = "app-static"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Demo application that statically links the C ABI socket library"

[dependencies]
socket-c = { path = "../socket-c" }
